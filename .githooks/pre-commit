#!/usr/bin/env bash
# fmt + clippy gate. CI runs the same check as the authoritative gate — this
# just catches drift earlier. Bypass with --no-verify.
set -euo pipefail
cargo fmt --check
cargo clippy --all-targets -- -D warnings
